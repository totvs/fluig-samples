<?xml version="1.0" encoding="UTF-8"?>
<!--
	Este projeto é um exemplo (sample-component).
	Para uso em sua solução, altere as propriedades de identificação
	(groupId, artifactId, version, name, description) e ajuste os módulos
	conforme a estrutura do seu projeto.

	Para mais detalhes sobre o POM:
	https://maven.apache.org/guides/introduction/introduction-to-the-pom.html
-->
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

	<modelVersion>4.0.0</modelVersion>
	<groupId>com.fluig</groupId>
	<artifactId>sample-component</artifactId>
	<version>${revision}</version>
	<packaging>pom</packaging>

	<name>Sample Component</name>
	<description>Sample Component description</description>

	<!-- Necessário configurar o nexus.fluig.com como repositório central de dependências -->
	<repositories>
		<repository>
			<id>nexus</id>
			<name>fluig Nexus Repository</name>
			<url>https://nexus.fluig.com/content/groups/public</url>
		</repository>
	</repositories>

	<properties>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
		<!--
			Revision is the single source of truth for the project version.
			Override via CLI: mvn package -Drevision=1.3.0
			Or update .mvn/maven.config to change the default.
		-->
		<revision>1.2.0</revision>
		<!-- TOTVS Fluig platform version this app was developed and tested against -->
		<fluig.version>2.0.0</fluig.version>
		<!-- Java compiler source/target version -->
		<java.version>11</java.version>
	</properties>

	<modules>
		<module>sample-component-layout</module>
		<module>sample-component-widget</module>
		<module>sample-component-config</module>
		<module>sample-component-entity</module>
		<module>sample-component-service</module>
		<module>sample-component-rest</module>
		<module>sample-component-pack</module>
	</modules>

	<!--
		Dependency Management: centralizes versions for all dependencies used across modules.
		Child modules declare dependencies WITHOUT <version> — version is resolved from here.
	-->
	<dependencyManagement>
		<dependencies>

			<!-- ================================================================ -->
			<!-- Java EE                                                          -->
			<!-- ================================================================ -->
			<dependency>
				<groupId>javax</groupId>
				<artifactId>javaee-api</artifactId>
				<version>8.0.1</version>
				<scope>provided</scope>
			</dependency>

			<!-- ================================================================ -->
			<!-- Fluig Platform — SDK & Foundation                               -->
			<!-- ================================================================ -->
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>fluig-sdk-api</artifactId>
				<version>${fluig.version}</version>
				<scope>provided</scope>
			</dependency>
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>fluig-sdk-common</artifactId>
				<version>${fluig.version}</version>
				<scope>provided</scope>
			</dependency>
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>fluig-sdk-keyring</artifactId>
				<version>${fluig.version}</version>
				<scope>provided</scope>
			</dependency>
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>fluig-api-tools</artifactId>
				<!--
					fluig-api-tools has its own versioning, independent of fluig.version.
					Check https://nexus.fluig.com for the latest available version.
				-->
				<version>1.1.3</version>
				<scope>provided</scope>
			</dependency>
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>foundation-common</artifactId>
				<version>${fluig.version}</version>
				<scope>provided</scope>
			</dependency>
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>foundation-cache-module</artifactId>
				<version>${fluig.version}</version>
				<scope>provided</scope>
			</dependency>

			<!-- ================================================================ -->
			<!-- Internal modules — version follows project revision             -->
			<!-- ================================================================ -->
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>sample-component-entity</artifactId>
				<version>${project.version}</version>
			</dependency>
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>sample-component-service</artifactId>
				<version>${project.version}</version>
				<type>ejb</type>
			</dependency>
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>sample-component-rest</artifactId>
				<version>${project.version}</version>
				<type>war</type>
			</dependency>
			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>sample-component-widget</artifactId>
				<version>${project.version}</version>
				<type>war</type>
			</dependency>

			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>sample-component-layout</artifactId>
				<version>${project.version}</version>
				<type>war</type>
			</dependency>

			<dependency>
				<groupId>com.fluig</groupId>
				<artifactId>sample-component-config</artifactId>
				<version>${project.version}</version>
				<type>war</type>
			</dependency>


			<!-- ================================================================ -->
			<!-- Third-party                                                      -->
			<!-- ================================================================ -->
			<dependency>
				<groupId>oauth.signpost</groupId>
				<artifactId>signpost-core</artifactId>
				<version>2.1.1</version>
			</dependency>

		</dependencies>
	</dependencyManagement>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<version>3.3.1</version>
				<configuration>
					<warSourceExcludes>**/.js,**/.css</warSourceExcludes>
					<failOnMissingWebXml>false</failOnMissingWebXml>
					<archive>
						<manifestEntries>
							<Dependencies>com.fluig.common</Dependencies>
						</manifestEntries>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<groupId>net.alchim31.maven</groupId>
				<artifactId>yuicompressor-maven-plugin</artifactId>
				<version>1.5.1</version>
				<executions>
					<execution>
						<goals>
							<goal>compress</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<excludes>
						<exclude>**/*.js</exclude>
					</excludes>
					<nosuffix>true</nosuffix>
					<force>true</force>
					<linebreakpos>-1</linebreakpos>
					<disableOptimizations>false</disableOptimizations>
					<gzip>false</gzip>
					<jswarn>false</jswarn>
					<nomunge>true</nomunge>
					<preserveAllSemiColons>false</preserveAllSemiColons>
				</configuration>
			</plugin>
			<plugin>
				<groupId>com.github.blutorange</groupId>
				<artifactId>closure-compiler-maven-plugin</artifactId>
				<version>2.21.0</version>
				<executions>
					<execution>
						<id>default-minify</id>
						<goals>
							<goal>minify</goal>
						</goals>
						<phase>generate-resources</phase>
					</execution>
				</executions>
				<configuration>
					<skip>true</skip>
					<baseSourceDir>${project.basedir}/src</baseSourceDir>
					<encoding>UTF-8</encoding>
					<sourceDir>main/webapp/resources</sourceDir>
					<targetDir>resources</targetDir>
					<includes>**/*.js</includes>
					<outputFilename>#{path}/#{basename}.#{extension}</outputFilename>
					<excludes>**/${project.basedir}</excludes>
					<excludes>**/*.min.js</excludes>
					<excludes>**/*-min.js</excludes>
					<closureCompilationLevel>SIMPLE_OPTIMIZATIONS</closureCompilationLevel>
					<closureWarningLevels>
						<undefinedVars>OFF</undefinedVars>
						<duplicate>OFF</duplicate>
						<duplicateMessage>OFF</duplicateMessage>
						<es5Strict>OFF</es5Strict>
						<checkVars>OFF</checkVars>
					</closureWarningLevels>
					<closureEmitUseStrict>false</closureEmitUseStrict>
					<closureRewritePolyfills>false</closureRewritePolyfills>
					<closureLanguageOut>NO_TRANSPILE</closureLanguageOut>
					<skipMinify>false</skipMinify>
					<skipMerge>true</skipMerge>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-compiler-plugin</artifactId>
				<version>3.2</version>
				<configuration>
					<source>${java.version}</source>
					<target>${java.version}</target>
				</configuration>
			</plugin>
		</plugins>

		<!--
			Plugin Management: centralizes plugin versions for all child modules.
			Child modules reference plugins WITHOUT <version> — resolved from here.
		-->
		<pluginManagement>
			<plugins>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-compiler-plugin</artifactId>
					<version>3.2</version>
					<configuration>
						<source>${java.version}</source>
						<target>${java.version}</target>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-war-plugin</artifactId>
					<version>3.3.1</version>
					<configuration>
						<failOnMissingWebXml>false</failOnMissingWebXml>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-ejb-plugin</artifactId>
					<version>3.1.0</version>
					<configuration>
						<ejbVersion>3.1</ejbVersion>
					</configuration>
				</plugin>
				<plugin>
					<groupId>org.apache.maven.plugins</groupId>
					<artifactId>maven-ear-plugin</artifactId>
					<version>2.10</version>
				</plugin>
				<plugin>
					<groupId>com.github.blutorange</groupId>
					<artifactId>closure-compiler-maven-plugin</artifactId>
					<version>2.21.0</version>
				</plugin>
				<plugin>
					<groupId>net.alchim31.maven</groupId>
					<artifactId>yuicompressor-maven-plugin</artifactId>
					<version>1.5.1</version>
				</plugin>
			</plugins>
		</pluginManagement>

		<finalName>${project.artifactId}</finalName>
	</build>

</project>